## Agent skills

### Issue tracker

Issues are tracked as local Markdown files under `.scratch/run-scoped-resource-capsule/`. See `docs/agents/issue-tracker.md`.

### Domain docs

This repository uses a single-context layout spanning `apps/server` and `apps/web`. See `docs/agents/domain.md`.

### Active effort

Before planning or implementing Run-scoped Resource Capsule, read:

- `docs/planning/resource-capsule-brief.md`
- `.scratch/run-scoped-resource-capsule/spec.md`, when it exists
- the assigned ticket under `.scratch/run-scoped-resource-capsule/issues/`, when implementation begins

Do not expand the effort beyond its approved brief or specification.
