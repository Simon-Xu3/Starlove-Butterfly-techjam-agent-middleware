# Inventory incident

The warehouse availability service reported stale stock counts after a
replenishment import. The incident bundle is a reproducible demo fixture for
the server-owned `inventory-incident` Protected Resource.
